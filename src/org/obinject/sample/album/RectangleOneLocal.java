package org.obinject.sample.album; import org.obinject.block.Page; import org.obinject.block.PullPage; import org.obinject.block.PushPage; import org.obinject.meta.generator.DistanceUtil; import org.obinject.meta.Rectangle; import org.obinject.meta.Uuid; import org.obinject.storage.KeyStructure; public class RectangleOneLocal extends $Local implements Rectangle<RectangleOneLocal> { private static Uuid classId; public static Uuid getClassId(){ if (RectangleOneLocal.classId == null) {RectangleOneLocal.classId = Uuid.fromString("88EA1160-FAF5-63CB-DB7C-9E37AE729AB3"); }return RectangleOneLocal.classId; } private double preservedDistance; private double extensions[] = new double[2];public RectangleOneLocal(){} public RectangleOneLocal($Local obj){ super(obj); } public RectangleOneLocal(Local obj, Uuid uuid){ super(obj, uuid); } public RectangleOneLocal(Uuid uuid){ super(uuid); } @Override public boolean hasSameKey(RectangleOneLocal obj) { int i=0; while(( i < this.numberOfDimensions()) && (this.getOrigin(i) == obj.getOrigin(i))){ i++; }return i == this.numberOfDimensions(); }@Override public KeyStructure<RectangleOneLocal> getKeyStructure() { return rectangleOneLocalStructure; } @Override public boolean pullKey(byte[] array, int position) { PullPage pull = new PullPage(array, position); for (int i = 0; i < this.numberOfDimensions(); i++){ this.setOrigin(i, pull.pullDouble()); this.setExtension(i, pull.pullDouble()); } return true; } @Override public void pushKey(byte[] array, int position){ PushPage push = new PushPage(array, position); for (int i = 0; i < this.numberOfDimensions(); i++){ push.pushDouble(this.getOrigin(i)); push.pushDouble(this.getExtension(i)); } } @Override public int sizeOfKey() { return Page.sizeOfDouble * 2 * this.numberOfDimensions();} @Override public double getOrigin(int axis){ switch(axis){ case 0: return getLatitudePadrao(); case 1: return getLongitudePadrao(); default: return 0; } } @Override public void setOrigin(int axis, double value){ switch(axis){ case 0: setLatitudePadrao(value); break; case 1: setLongitudePadrao(value); break; } } @Override public int numberOfDimensions(){ return 2;} @Override public double getExtension(int axis){ return extensions[axis]; } @Override public void setExtension(int axis, double value){ extensions[axis]=value; } @Override public double getPreservedDistance() { return this.preservedDistance; } @Override public void setPreservedDistance(double distance) { this.preservedDistance = distance; } @Override public double distanceTo(RectangleOneLocal obj){ return DistanceUtil.euclidean(this, obj); } } 